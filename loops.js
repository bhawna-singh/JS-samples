let arr=[12,13,15]; 
console.log(arr);
// heterogenous array

let arr1=[14,"kiwi",false];
console.log(arr1.length);

// for loop
for(var i=0;i<arr.length;i++) 
{console.log(arr[i]);}

// for of loop
for(let val of arr)
{console.log(val);}

//index in loop
for(let index in arr1)
{console.log(index);
   
}